using UnityEngine;

public enum CollectableType
{
    HealthPickUp,
    Character,
    StatModifier,
    PartyIncrease, // Which is also like a stat modifier internally
    // Buff
    // Upgrade
    // etc...
}
